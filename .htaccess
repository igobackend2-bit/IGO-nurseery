<IfModule mod_rewrite.c>
  RewriteEngine On

  # Allow existing files/directories to load normally
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Route all SPA paths to index.html
  RewriteRule ^ index.html [L]
</IfModule>
